# NearSip-Dashboard

Next.js + TypeScript migration of the NearSip dashboard prototype. The current
data is deterministic mock data and is clearly marked as non-production data.

## Run locally

```bash
bun install
bun run dev
```

Open <http://localhost:3000>.

## Validation

```bash
bun run typecheck
bun run build
```
